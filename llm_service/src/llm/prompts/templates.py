"""各 Agent 的 prompt 模板集中管理。

使用 str.format 风格的占位符，由各 Agent 填充后送入 LLM。

为 8 个 Agent 各提供 1 个显式命名的 prompt 模板：
- LITERATURE_PROMPT  文献调研
- DESIGN_PROMPT      实验设计
- EXPERIMENT_PROMPT  实验执行（模拟）
- EVALUATE_PROMPT    结果评价
- DISCUSS_PROMPT     讨论
- WRITE_PROMPT       论文写作（按章节）
- FIGURE_PROMPT      图表规格
- SUBMIT_PROMPT      投稿包

旧名（带后缀）保留为别名，避免破坏既有代码。
"""

from __future__ import annotations

# ============================================================================
# 1) 文献调研
# ============================================================================

LITERATURE_PROMPT = """你是一位科研文献调研专家。

【科研问题】
{question}

【学科领域】
{discipline}

【已有线索 / RAG 检索结果】
{context}

请基于以上信息，完成：
1. 总结当前研究现状（约 300 字）
2. 列出关键研究缺口
3. 推荐 3-5 个值得深入阅读的研究方向
4. 用 JSON 数组返回 3-5 条最相关的文献线索，每条包含字段：
   title, abstract(<=150字), year, source

请以结构化中文输出，不要编造不存在的文献。
"""

# 旧名别名
LITERATURE_SEARCH_PROMPT = LITERATURE_PROMPT


# ============================================================================
# 2) 实验设计
# ============================================================================

DESIGN_PROMPT = """你是一位研究设计专家，精通{discipline_label}领域的方法学。

【科研问题】
{question}

【学科领域】
{discipline}

【学科方法学要求】
- 方法规范：{method_requirement}
- 证据规范：{evidence_requirement}
- 可重复性：{reproducibility_requirement}

【文献调研结论】
{literature_summary}

请先判断该问题属于实验研究、计算研究、证据综合、案例研究还是社会影响评价，再设计一套与科研问题紧密相关的研究方案。不得仅凭学科名称强行套用实验室流程。

学科特定要求：
{discipline_hint}

请设计以下内容：
1. 研究假设（hypothesis）：必须与科研问题直接对应，且可证伪
2. 变量（variables）：实验研究填写自变量、因变量和控制变量；综述/案例/社会影响研究填写分析维度、结果维度和情境边界
3. 数据集 / 资源（dataset）：数据、样本、文献、案例、统计资料或其他证据来源
4. 方法步骤（method_steps）：按顺序列出，每步需具体可操作，并符合所选研究类型
5. 评估指标（metrics）：必须使用本学科常用指标，不得使用其他学科的不相关指标

请严格用如下 JSON 输出（不要任何额外文字）：
{{
  "hypothesis": "...",
  "variables": {{"independent": [...], "dependent": [...], "control": [...]}},
  "metrics": [...],
  "dataset": "...",
  "method_steps": ["step1", "step2", ...]
}}
"""

# 旧名别名
EXPERIMENT_DESIGN_PROMPT = DESIGN_PROMPT


# ============================================================================
# 3) 实验执行（模拟）— 增强版：跨学科结构化实验方案
# ============================================================================

EXPERIMENT_PROMPT = """你是一位严谨的{discipline_label}研究执行者。当前为模拟环境（无真实算力、实验条件或现场调查），请基于研究方案生成结构化、符合学科规范的执行过程与待验证结果框架。

【科研问题】
{question}

【学科领域】
{discipline}

【学科研究规范】
- 方法规范：{method_requirement}
- 证据规范：{evidence_requirement}
- 可重复性：{reproducibility_requirement}

【研究方案】
{experiment_design}

【可用数据/材料/证据描述】
{dataset_hint}

请先遵循研究方案已经选择的方法类型，再形成执行过程。综述、案例研究和社会影响评价不得改写成实验室实验；所有内容必须与科研问题直接相关。

学科特定要求：
{discipline_hint}

输出字段要求：
1. methodology：研究方法/设计描述（2-3 句，必须符合已选择的研究类型）
2. materials：实验材料、数据、文献、案例、统计资料或其他证据来源
3. procedure：研究步骤（按顺序编号，5-8 步，每步需具体可操作）
4. metrics：结构化指标列表，每个指标含 name/value/unit/note 字段
   - 指标名称必须使用本学科常用指标
   - 没有真实输入数据时使用“待收集/待编码/待评价”，不得编造看似真实的数值
   - 需包含至少 3 个指标
   - 如适用，在 note 中注明基线对比
5. resultsDescription：结果或证据综合框架（3-5 句；只有输入包含真实数值时才能引用关键数值）
6. rawLogs：模拟执行记录（8-15 行，记录检索、处理、分析或实验过程）

注意：
- 不得编造不存在的数据集、设备、调查、访谈、案例或统计结果
- 没有真实数据时不得生成伪精确数值；使用待验证状态和规范的评价字段
- 所有模拟数据必须标注"模拟"字样

请严格用如下 JSON 输出（不要任何额外文字）：
{{
  "source": "agent",
  "methodology": "...",
  "materials": "...",
  "procedure": "...",
  "metrics": [
    {{"name": "...", "value": "...", "unit": "...", "note": "..."}}
  ],
  "resultsDescription": "...",
  "rawLogs": "..."
}}
"""


# ============================================================================
# 4) 结果评价
# ============================================================================

EVALUATE_PROMPT = """你是一位严谨的实验结果评审专家。

【科研问题】
{question}

【实验方案】
{experiment_design}

【实验结果】
{experiment_results}

请从以下维度评价实验结果：
1. 有效性（是否支持假设）
2. 鲁棒性
3. 与基线对比
4. 局限性
5. 改进建议

请严格用如下 JSON 输出（不要任何额外文字）：
{{
  "summary": "（约 200 字的总体评价）",
  "table": [["指标", "数值", "评价"], ...],
  "comparison": ["与基线 A 相比 ...", "与基线 B 相比 ..."],
  "limitations": ["..."],
  "improvements": ["..."]
}}
"""

# 旧名别名
EVALUATION_PROMPT = EVALUATE_PROMPT


# ============================================================================
# 5) 讨论
# ============================================================================

DISCUSS_PROMPT = """你是一位学术论文讨论撰写专家。

【科研问题】
{question}

【实验结果】
{experiment_results}

【评价结论】
{evaluation_summary}

请撰写论文的"讨论"部分，包含：
1. 主要发现
2. 与已有工作的对比
3. 理论与实践意义
4. 局限性
5. 未来工作

请输出约 600 字的中文学术段落（不要 JSON）。
"""

# 旧名别名
DISCUSSION_PROMPT = DISCUSS_PROMPT


# ============================================================================
# 6) 论文写作（按章节）
# ============================================================================

WRITE_PROMPT = """你是一位学术论文写作专家。

【科研问题】
{question}

【学科领域】
{discipline}

【文献调研】
{literature_summary}

【实验方案】
{experiment_design}

【实验结果】
{experiment_results}

【评价结论】
{evaluation_summary}

【当前需要撰写的章节】
{section}

请仅撰写该章节，要求：
- 学术化中文
- 长度 200~500 字
- 逻辑清晰、结构完整
- 不编造数据与文献
"""

# 旧名别名
PAPER_WRITE_PROMPT = WRITE_PROMPT

# 论文章节定义（与 WRITE_PROMPT 配套使用）
PAPER_SECTIONS = {
    "abstract": "摘要（约 200 字，含背景、方法、结果、结论）",
    "intro": "引言（研究背景、问题、贡献）",
    "method": "方法（实验设计、数据、流程）",
    "results": "结果（实验结果呈现与分析）",
    "discussion": "讨论（发现、对比、局限、未来工作）",
    "conclusion": "结论（核心贡献与展望）",
}


# ============================================================================
# 7) 图表（占位）
# ============================================================================

FIGURE_PROMPT = """你是一位科研论文图表设计专家。

【科研问题】
{question}

【实验结果】
{experiment_results}

【需要生成的图表描述】
{figure_request}

请为论文推荐 3-5 张关键图表（line / bar / scatter / table），每张含 id/type/caption/data/code 字段。

请严格用如下 JSON 数组输出（不要任何额外文字）：
[
  {{"id": "fig_1", "type": "line", "caption": "...", "data": {{...}}, "code": "..."}},
  ...
]
"""


# ============================================================================
# 8) 投稿包
# ============================================================================

SUBMIT_PROMPT = """你是一位学术投稿顾问。

【科研问题】
{question}

【论文摘要】
{abstract}

【学科领域】
{discipline}

请推荐：
1. 3-5 个合适的目标期刊 / 会议（含理由）
2. 投稿材料清单（checklist）
3. Cover Letter 要点

请严格用如下 JSON 输出（不要任何额外文字）：
{{
  "target_venue": [
    {{"name": "...", "tier": "Q1/Q2/...", "reason": "..."}}
  ],
  "checklist": ["item1", "item2", ...],
  "cover_letter": "（cover letter 草稿要点，约 200 字）"
}}
"""


# ============================================================================
# 9) 论文写作前的深度思考（reasoning 模型专用）
# ============================================================================

THINKING_PROMPT = """你是一位资深学术论文写作架构师。在开始撰写论文之前，请对以下研究内容进行深度思考和规划。

【研究问题】
{question}

【学科】
{discipline}

【文献综述】
{literature}

【实验设计】
{design}

【实验结果】
{results}

【评价结论】
{evaluation}

【讨论要点】
{discussion}

【目标总字数】
约 {word_limit} 个中文字符

请仔细思考以下问题，并输出你的思考结果：

1. 核心论点：这篇论文的核心贡献是什么？用一句话概括。
2. 论证逻辑链：从文献缺口→研究问题→方法→结果→讨论→结论的完整论证链是什么？请确保每一环都能自洽。
3. 章节分配策略：基于目标字数 {word_limit} 字，各章节应如何分配字数？哪些章节需要重点展开？
4. 数据与证据映射：实验结果中的哪些指标支撑论文的哪个论点？指标与论点之间的对应关系是什么？
5. 潜在审稿人质疑：审稿人可能从哪些方面质疑本文？如何在正文中提前回应？
6. 术语统一：全文需要统一的关键术语有哪些？
7. 模拟数据标注策略：哪些内容是模拟数据？如何在正文中恰当标注而不削弱论文说服力？

请以结构化文本输出你的思考结果（不需要 JSON 格式），每个问题用编号标题开头，内容简洁有力。"""
